// const { protocol, hostname } = window.location;
const { hostname } = window.location;
const protocol = 'https:';
const djangoPort = 8000;
const nodePort = 4000;

export { protocol, hostname, djangoPort, nodePort };